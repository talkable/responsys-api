require 'spec_helper.rb'

describe Member do
  context "#subscribed?" do
    it "should return true if the user is subscribed to the Responsys list specified" do
    end
  end

  context "#unsubscribe" do
    it "should unsubscribe a user who is opted in to receiving emails" do
    end

    it "should do nothing if the user has already unsubscribed" do
    end
  end

  context "#retrieve_information_by_email" do
    it "should retrieve the user information specfied from the list specified" do
    end
  end
end
